version: '3.7'

services:
  nginx-stage:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx-stage
    restart: always
    volumes:
      - ./dist:/usr/share/nginx/html
    ports:
      - 8080:80
