version: '3.7'

services:
  nginx-prod:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nginx-prod
    restart: always
    volumes:
      - ./dist:/usr/share/nginx/html
    ports:
      - 80:80
